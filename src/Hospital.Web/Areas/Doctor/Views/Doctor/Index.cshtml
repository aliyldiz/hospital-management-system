@using Hospital.Utilities
@model PagedResult<Hospital.ViewModels.TimingViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Timing</h1>

<p>
    <a asp-action="AddTiming">Create New</a>
</p>
<hr />
<table class="table">
    <thead>
    <tr>
        <th>
            Id
        </th>
        <th>
            ScheduleDate
        </th>
        <th>
            MorningShiftStartDate
        </th>
        <th>
            MorningShiftEndDate
        </th>
        <th>
            AfternoonShiftStartDate
        </th>
        <th>
            AfternoonShiftEndDate
        </th>
        <th>
            Duration
        </th>
        <th>
            Status
        </th>
        <th>
            DoctorId
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Data)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScheduleDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MorningShiftStartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MorningShiftEndTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AfternoonShiftStartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AfternoonShiftEndTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DoctorId)
            </td>
            <td>
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }
    </tbody>
</table>

<cs-pager cs-paging-pagesize=(int)Model.PageSize 
          cs-paging-pagenumber=(int)Model.PageNumber 
          cs-paging-totalitems=(int)Model.TotalItems
          cs-pagenumber-param="pageNumber"
          asp-controller="Timings"
          
          asp-action="Index"
          cs-pager-li-current-class="page-item active"
          cs-pager-li-other-class="page-item"
          cs-pager-li-non-active-class="page-item disabled"
          cs-pager-link-current-class="page-link"
          cs-pager-link-other-class="page-link"
></cs-pager>